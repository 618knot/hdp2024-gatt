```sh
sudo apt install -y libdbus-1-dev libdbus-glib-1-dev libgpiod-dev libffi-dev libcairo2-dev python3-gi gir1.2-gtk-3.0 python3-rpi.gpio libgirepository1.0-dev gobject-introspection
python3 -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
```
